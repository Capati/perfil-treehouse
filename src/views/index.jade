extends ./layout

block content
  if (msgErro)
      include ./parciais/erro
  img(src="/img/buscar.png" alt="Ícone de Lupa" id="iconeBuscar")

  form(action="/perfil" method="POST")
    input(type="text" placeholder="Digite um nome de usuário" id="usuario" name="usuario" required)
    input(type="submit" value="Buscar" id="botaoBuscar")
